import config from "../config";
import amplitude from 'amplitude-js';

// amplitude.getInstance().init(config.apmlitude_key, undefined, {batchEvents: false});

// export default amplitude.getInstance();
